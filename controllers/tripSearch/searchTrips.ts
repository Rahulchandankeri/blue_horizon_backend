import { NextFunction, Request, Response } from 'express';

exports.searchBus = async (req: Request, res: Response, next: NextFunction) => {
  const { from, to, fromDate } = req.body;

  try {
  } catch (error) {}
};
